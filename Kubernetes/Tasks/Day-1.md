# Kubernetes Practical Assignment  
**Cluster:** devhrk8sm1  
**Instructions:**  
- Complete all tasks using **kubectl commands only**.  
- Use **official Kubernetes documentation** as reference.  
- **Do NOT use GPT or any AI tool** for solving the tasks.  
- Show the commands used and the outputs/screenshots as part of your submission.  

---

## 📌 Task 1: List All Namespaces  
Retrieve and document the complete list of namespaces in the cluster **devhrk8sm1**.

---

## 📌 Task 2: List All Nodes & Identify Master/Worker Nodes  
Using only `kubectl` commands:  
1. Get the full list of nodes.  
2. Identify how many **master nodes** and **worker nodes** are present.  
3. Document the **configuration details** of each node (CPU, memory, roles, etc.).

---

## 📌 Task 3: Count Deployments in Every Namespace  
For each namespace in the cluster:  
- Retrieve the number of deployments.  
- Present the results clearly, namespace-wise.

---

## 📌 Task 4: Top 10 Pods by CPU Usage (Per Namespace)  
For **every namespace**:  
- Retrieve the **top 10 pods** consuming the highest CPU usage.  
- Present the list in a clear and comparable format.  

---

## 📌 Task 5: Top 10 Pods by Memory Usage (Per Namespace)  
For **every namespace**:  
- Retrieve the **top 10 pods** consuming the highest memory usage.  
- Present the list similarly to Task 4.

---

### ⚠️ Reminder  
- **Do not use GPT or AI tools.**  
- Only use **kubectl** + **Kubernetes official documentation**.

---
